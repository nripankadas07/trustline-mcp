# Golden expectation

`expected.json` records stable contract-level values for the checked-in policy and attack transcript. Run `npm run demo`; its totals and audit root must match this file. Update it only when an intentional fixture or artifact-contract change is documented in the changelog.
